const Philosophy = () => (
  <section className="section-shell" aria-label="Philosophy">
    <div className="mx-auto max-w-5xl text-center">
      <h2 className="section-title mb-8 font-semibold text-[clamp(1.25rem,3vw,2.25rem)]">Software should reduce operational friction, not create more work.</h2>
      <p className="mx-auto max-w-3xl text-[clamp(0.95rem,1.5vw,1.125rem)] leading-relaxed text-[var(--text-muted)]">
        I design systems around how businesses actually operate - from stock movement and billing to reporting, communication, and deployment.
      </p>
    </div>
  </section>
);

export default Philosophy;
